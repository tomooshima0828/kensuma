<aside class="main-sidebar sidebar-dark-primary elevation-4">
  <!-- Brand Logo -->
  <%= link_to users_dash_boards_path, class:"brand-link", style: "display: flex" do %>
    <%= image_tag "kensuma/03", class:"brand-image elevation-3", style: "opacity: .8" %>
    <%# <span class="brand-text font-weight-light">建スマ</span> %>
  <% end %>

  <!-- Sidebar -->
  <div class="sidebar">
    <!-- Sidebar user panel (optional) -->
    <div class="user-panel mt-3 pb-3 mb-3 d-flex">
      <div class="image">
        <%#= image_tag "user2-160x160.jpg", class:"img-circle elevation-2" %>
      </div>
      <div class="info">
        <%= link_to current_user.name, users_profile_path, class:"d-block" %>
        <%= link_to "ログアウト", destroy_user_session_path, class:"d-block mt-3", method: :delete %>
      </div>
    </div>

    <!-- Sidebar Menu -->
    <nav class="mt-2">
      <ul class="nav nav-pills nav-sidebar flex-column" data-widget="treeview" role="menu" data-accordion="false">
        <li class="nav-item">
          <%= link_to users_dash_boards_path, class:"nav-link" do %>
            <i class="nav-icon fas fa-angle-left"></i>
            <p>ダッシュボードに戻る</p>
          <% end %>
        </li>
      </ul>
      <ul class="nav nav-pills nav-sidebar flex-column" data-widget="treeview" role="menu" data-accordion="false">
        <!--
        <%# @cover_documents.each do |cover_document| %>
          <%#= link_to users_document_path(cover_document), class:"nav-link" do %>
          <li class="nav-item">
            <i class="nav-icon fas fa-file"></i>
            <p><%#= Document.human_attribute_name(:id) %> <%#= cover_document.id %></p>
          </li>
          <%# end %>
        <%# end %>
        -->
        <% @documents.each do |request_document| %>
          <%= link_to users_request_order_document_path(params[:request_order_uuid], request_document.uuid), class:"nav-link" do %>
          <li class="nav-item">
            <i class="nav-icon fas fa-file"></i>
            <p><%= request_document.document_type_i18n %></p>
            <p><%= request_document.uuid %></p>
          </li>
          <% end %>
        <% end %>
      </ul>
    </nav>
    <!-- /.sidebar-menu -->
  </div>
  <!-- /.sidebar -->
</aside>
