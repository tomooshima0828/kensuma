<% provide(:title,  "お知らせ") %>
<div class="content">
  <div class="container-fluid">
    <div class="row">
      <% @news_all.each do |news| %>
      <ul>
        <li><%= link_to news.title, users_news_path(news) %></li>
        <li><%= news.content %></li>
        <li>配信日時: <%= l(news.delivered_at, format: :long) %></li>
        <li><%= news.status %></li>
      </ul>
      <% end %>
    </div>
    <!-- /.row -->
  </div><!-- /.container-fluid -->
</div>
